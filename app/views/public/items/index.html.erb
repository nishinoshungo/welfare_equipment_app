<h1>レンタル商品一覧</h1>
<% @items.each do |item| %>
  <%= link_to "/items/#{item.id}" do %>
    <%= attachment_image_tag item, :image, :fill, 100, 100, format: 'jpg' %>
  <% end %>
  <%= item.name  %>
  <%= item.price %>円
<% end %>
